#include <iostream>

using namespace std;

int main()
{
    int a;
    cout<<"enter a integer: ";
    cin>>a;
    if(a%2==0)
        cout<<"the number is even.";
    if(a%2!=0)
        cout<<"the number is odd";
    return 0;
}
